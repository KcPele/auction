# =============================================================================
# Frontend environment
# Copy to .env.local for local dev. Never commit .env.local.
# =============================================================================

# -----------------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------------

# Server-only. Where Next.js forwards proxied /api/v1/* requests.
# The browser never sees this value — it stays out of the JS bundle.
# Local dev: backend running on :4000.
# Staging:   https://api-staging.kcpele.com
# Prod:      https://api-auction.kcpele.com
API_ORIGIN=http://localhost:4000

# Browser-visible WebSocket endpoint for the live bid + notifications gateways.
# Bypasses the Next.js rewrite — long-lived WS connections must hit the backend
# origin directly (Next rewrites buffer responses and break sockets in prod).
# Local dev:   http://localhost:4000
# Staging/prod: dedicated subdomain like https://ws.api-auction.kcpele.com
NEXT_PUBLIC_WS_URL=http://localhost:4000

# -----------------------------------------------------------------------------
# Optional (uncomment when the corresponding feature is wired)
# -----------------------------------------------------------------------------

# Public app URL. Used for absolute URLs in metadata, OG images, redirects.
# Must match the origin Better Auth sends as `redirectTo` in reset-password emails.
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Payments mode for the wallet UI.
#   sandbox (default): show the "Simulate payment" button on the funding screen
#                      so QA can credit wallets without a real bank transfer.
#                      Backend STILL gates the simulate endpoint behind
#                      STROWALLET_MODE=sandbox, so flipping this client-side
#                      alone does NOT enable free money in prod.
#   live:              hide the simulate button entirely.
# NEXT_PUBLIC_PAYMENTS_MODE=sandbox
